namespace SqlBob
{
    public static class Join
    {
        ///// <summary>Creates a JOIN based on a raw SQL <see cref="string"/>.</summary>
        //[Pure]
        //public static Raw Raw(string value) => value;

        ///// <summary>Creates an INNER JOIN.</summary>
        //[Pure]
        //public static Raw Inner(object expression) => new(expression);

        ///// <summary>Creates an LEFT (OUTER) JOIN.</summary>
        //[Pure]
        //public static Raw Left(object expression) => new(expression);

        ///// <summary>Creates an RIGHT (OUTER) JOIN.</summary>
        //[Pure]
        //public static Raw Right(object expression) => new(expression);

        ///// <summary>Creates an FULL OUTER JOIN.</summary>
        //[Pure]
        //public static Raw FullOuther(object expression) => new(expression);
    }
}
